import styled from "styled-components"

export const AppContainer = styled.main`
    width: 100%;
    min-height: 100vh;

    margin: 0 auto;

    @media (min-width: 700px) {
        width: 700px;
        margin: 0 auto;
    }
`
